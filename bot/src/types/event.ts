export interface EventData {
  title: string;
  gameLabel: string;
  tagline: string;
  dateLabel: string;
  durationLabel: string;
  accent: string; // #RRGGBB theme colour for the minigame
  serverName: string;
}
